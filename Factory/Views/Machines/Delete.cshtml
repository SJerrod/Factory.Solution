@{
  Layout = "_Layout";
}

@model Factory.Models.Machine;

<h2>Are you sure you want to delete this Machine?</h2>

@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)
@using (Html.BeginForm())
{
    <button type="submit"><span class="glyphicon glyphicon-ban-circle" aria-hidden="true"></span> Delete</button>
}
<button onclick="window.history.back()"><span class="glyphicon glyphicon-backward" aria-hidden="true"></span> Back</button>