@{
  Layout = "_Layout";
}

@model Factory.Models.Machine;

<h4>Add a Machine</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <button type="submit"><span class="glyphicon glyphicon-cog" aria-hidden="true"></span> Add Machine</button>
}
